import 'package:flutter/material.dart';

class AppColors {
  // ===== Brand =====
  static const primary = Color(0xFF0F766E); // Deep Teal Green
  static const primaryDark = Color(0xFF115E59);

  static const secondaryLight = Color(0xFF84CC16); // Lime Green
static const secondaryDark = Color(0xFF65A30D);  // Dark Lime

  static const success = Color(0xFF16A34A);
  static const warning = Color(0xFFEAB308);
  static const error = Color(0xFFDC2626);
  static const info = Color(0xFF4ADE80); // Soft Green (not blue)

  // ===== Light Theme =====
  static const lightBackground = Color(0xFFFAFAF9); // Warm off-white
  static const lightSurface = Color(0xFFFFFFFF);
  static const lightBorder = Color(0xFFE7E5E4);

  static const lightTextPrimary = Color(0xFF1C1917); // Charcoal
  static const lightTextSecondary = Color(0xFF44403C);
  static const lightTextMuted = Color(0xFFA8A29E);
  static const lightTextOnPrimary = Color(0xFFF0FDFA);
  static const lightTextOnSecondary = Color(0xFFFFFBEB);
  static const lightIcon = Color(0xFF292524);
  static const lightDisabled = Color(0xFFD6D3D1);

  // ===== Dark Theme =====
  static const darkBackground = Color(0xFF0C0A09); // Warm dark
  static const darkSurface = Color(0xFF1C1917);
  static const darkBorder = Color(0xFF292524);

  static const darkTextPrimary = Color(0xFFFAFAF9);
  static const darkTextSecondary = Color(0xFFD6D3D1);
  static const darkTextMuted = Color(0xFFA8A29E);
  static const darkTextOnPrimary = Color(0xFFCCFBF1);
  static const darkTextOnSecondary = Color(0xFFFEF3C7);
  static const darkIcon = Color(0xFFE7E5E4);
  static const darkDisabled = Color(0xFF78716C);

  // ===== Containers (Material 3) =====
  static const primaryContainer = Color(0xFFCCFBF1); // Light teal
  static const onPrimaryContainer = Color(0xFF042F2E);

  static const secondaryContainer = Color(0xFFFFF3C4); // Soft amber
  static const onSecondaryContainer = Color(0xFF78350F);

  static const errorContainer = Color(0xFFFEE2E2);
  static const onErrorContainer = Color(0xFF7F1D1D);
}
